- [x] Fix grid deep link for modal display mode (fieldCount >= 10)
  - [x] Make `handleRowClick` always navigate to update URL regardless of display mode
  - [x] Make Dialog (modal) URL-driven — open when URL has recordId AND useModal condition is met
  - [x] Unified `handleClose` navigates back to list URL for both sheet and modal
  - [x] Update `onEditModal` callback to navigate instead of only setting state
  - [x] Conditionally show Sheet only when NOT in modal mode
  - [x] Remove unnecessary `modalMode`/`modalRecord` state
- [x] Build passes
- [x] Deployed and verified on Cloudflare preview

## Preview

https://fixcrmcust-20260228105700-semantius-web.ma532.workers.dev

## Screenshots

![Customer modal with deep link URL after clicking row](https://github.com/user-attachments/assets/757aae2e-c709-4014-950b-f273aa088afe)

## CONTEXT-MEMORY.md

No update needed — the fix applies a straightforward pattern (URL-driven dialog state) already used by the Sheet component in the same file. No new architectural knowledge involved.
