- [x] Prefix page heading with parent record label (muted + linked) when `_pf` is in URL
- [x] Exclude `_pf` column from DataTableView grid when `_pf` is in URL
- [x] Change `parentField` from `readonly` to `hidden` in `SchemaForm.tsx`
- [x] Filter out `is_child=true` entities from left-hand sidebar navigation

## Preview

https://refactorus-20260301224306-semantius-web.ma532.workers.dev

## Screenshots

### Fields list page — heading prefixed with "User" (muted link) and `table_name` column excluded from grid

![Fields list with User prefix heading and table_name column excluded](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/refactor-user-fields-display/screenshots/20260301223000-fields-with-pf-heading.png)

### Add Field form — table_name field is hidden (no longer shown as readonly)

![Add Field form with hidden table_name field](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/refactor-user-fields-display/screenshots/20260301223001-add-field-hidden-table-name.png)

## CONTEXT-MEMORY.md

No update needed — all changes are straightforward UI/filter refinements using existing patterns already documented in the codebase.
