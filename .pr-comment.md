- [x] Add `'page'` to `edit_mode` type in `metadata.ts`
- [x] Add `ChildRelation` interface and `children` field to `EntityMetadata` in `metadata.ts`
- [x] Update `SchemaForm.tsx` to accept `id` and `onBeforeSubmit` props
- [x] Update `TableForm.tsx` to accept `formId` and `onBeforeSubmit` props and pass to `SchemaForm`
- [x] Fix pre-existing TypeScript error in `NavApps.tsx` (`unknown && <img>` → `!!unknown && <img>`)
- [x] Update `View.tsx` with single `navigateForEditMode(mode, record?)` function
- [x] `StandaloneFormView`: render right-aligned `children` buttons above the form; save + redirect to `/` on child button click

## Preview

https://updateedit-20260301184909-semantius-web.ma532.workers.dev

## Screenshots

**Standalone page form with "User Roles..." children button (visiting `/_core/users/1002/view`):**

![Standalone user page form showing User Roles... child action button in top-right](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/update-edit-mode-forms/screenshots/20260301185213-user-page-view-children-buttons.png)

**New record page (`/new` route for page edit_mode):**

![New user page at /_core/users/new showing full-page create form](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/update-edit-mode-forms/screenshots/20260301185220-user-new-page.png)

**Sidebar overlay unchanged for auto mode (clicking Edit on a user row):**

![User edit form opening in sidebar overlay for auto edit_mode](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/update-edit-mode-forms/screenshots/20260301185244-user-sidebar-edit.png)

## CONTEXT-MEMORY.md

No update needed — all changes are self-evident from the code; no new architectural patterns or environmental quirks discovered.
