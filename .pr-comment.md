- [x] Fix breadcrumb module name — use actual module name (e.g. "CRM") from auth context instead of capitalising the URL segment ("crm" → "Crm")
- [x] Add breadcrumb to grid/list view — e.g. `/crm/customers` now shows "CRM > Customers"
- [x] Inline create — "Add" button opens an inline sheet/modal instead of navigating to the standalone `/new` route; the standalone `/new` route is still accessible
- [x] Fix table overflow — `min-w-0 overflow-x-hidden` on the AppLayout content div prevents the browser window from showing a horizontal scrollbar; the DataTable's own `overflow-auto` still provides internal horizontal scrolling
- [x] Fix search box sizing — left toolbar container is now `flex-1` so the search box expands to fill available space, capped at `max-w-[200px]` on wider screens

## Preview

https://fixbreadcr-20260228184944-semantius-web.ma532.workers.dev

## Screenshots

**CRM Customers grid — correct "CRM > Customers" breadcrumb, "Add Customer" button, full-width search box:**
![CRM customers grid with breadcrumb and Add Customer button](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/fix-breadcrumb-labels-and-layout/screenshots/20260228185109-crm-customers-grid.png)

**Entities page — no browser horizontal scrollbar, table contained within viewport:**
![Entities page with overflow fix](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/fix-breadcrumb-labels-and-layout/screenshots/20260228185130-entities-overflow.png)

**Inline create modal — "Add Customer" opens "New Customer" dialog inline:**
![Inline create modal](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/fix-breadcrumb-labels-and-layout/screenshots/20260228185231-inline-create.png)

## CONTEXT-MEMORY.md

No update needed — all changes are self-evident from well-commented code; no new architectural patterns, environment constraints, or cross-cutting conventions were introduced.
