- [x] Fix grid sort icon: hide unsorted ↑↓ icon until hover, always show active sort indicator (A→Z, Z→A, 0→1, 1→0)
- [x] Fix delete confirmation: show `Label (id)` format — e.g. **"Latin America (4)"** instead of just the ID
- [x] Add Sonner toast (top-right) via shadcn — fires on record created and record deleted
- [x] Fix column max-width: add `size` to column definitions from metadata width bucket so columns don't stretch to fill the screen

## Preview

https://updategrid-20260303090146-semantius-web.ma532.workers.dev

## Screenshots

**Regions grid — columns constrained to metadata widths, unsorted columns hide their sort icon:**
![Regions grid with constrained column widths and hover-only sort icons](https://github.com/user-attachments/assets/1cda3044-6cf6-46a8-9418-f826a418e51c)

**Delete confirmation — shows "Label (id)" format:**
![Delete dialog showing "Latin America (4)"](https://github.com/user-attachments/assets/d1c86a8f-0071-4b96-bc66-95c57754cd01)

## CONTEXT-MEMORY.md

No update needed — changes are self-contained UI improvements (sort icon visibility, delete label format, Sonner toast, column width). No new architectural patterns or platform constraints were discovered.
