- [x] Children buttons visible in Sheet/Dialog overlays (previously only on `/view` route)
- [x] Child navigation includes `_pf`/`_pv` parent-filter params
- [x] `_pv` is a plain unquoted string in the URL (`_pv=1002`, not `_pv=%221002%22`)
- [x] Fix React error #300: move `useRef` before `isStandalone` early-return (hooks must be unconditional)
- [x] Extract `buildChildNavUrl` helper to eliminate duplication
- [x] Document TanStack Router search-param JSON-encoding quirk in CONTEXT-MEMORY.md

## Preview

https://updateedit-20260301192849-semantius-web.ma532.workers.dev

## Screenshots

**"User Roles..." button in sidebar at `/_core/users/1002`, navigates to `/_core/user_roles?_pf=user_roles.user_id&_pv=1002` (clean, no JSON encoding):**

![user_roles table filtered by parent user, showing _pf and _pv as plain URL params](https://raw.githubusercontent.com/IntranetFactory/semantius-web/copilot/update-edit-mode-forms/screenshots/20260301193115-user-roles-clean-url.png)

## CONTEXT-MEMORY.md

Updated — added "TanStack Router Search Param Serialization" section explaining that strings which are valid JSON (like `'1002'`) get JSON-encoded with quotes, and documenting the `router.history.push()` workaround for `_pf`/`_pv` parent-filter params.
